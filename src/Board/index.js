import React from 'react';

import Map from '../Map';
import Modal from '../Modal';

const Board = () => {
  return (
    <div className="main">
      <Map />
      <Modal />
    </div>
  );
};

export default Board;
